pipeline{
    agent any
    stages{
        stage('SECRET_TEXT'){
            steps {
                    withCredentials([string(credentialsId: 'secrect_text', variable: 'SECRET_TEXT')])
                    echo "$SECRET_TEXT"
                     }
                }
                           
        }
}


